package controller;

//This application uses the MVC-architecture.
//The model contains all logic, view handles the GUI and controller handles the
//communication between the model and view.
//Model and view do not directly communicate.
public class MainProgram {
    public static void main(String[] args) {
        new Controller();
    }
}
